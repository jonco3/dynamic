dynamic
=======

A toy implementation of the python programming language in C++.

Building
--------

  $ autoreconf
  $ ./configure
  $ make test
  $ ./dynamic -l lib
  > print("Hello world")
  Hello world

Some useful configure options:

  --enable-debug  -- build debug version, no optimisation
  --with-ccache   -- use compiler cache if installed
