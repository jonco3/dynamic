#ifndef __NAME_H__
#define __NAME_H__

/*
 * Currently just use std::sring for names, but this could be changed to
 * something more lightweight.
 */

#include <string>

using namespace std;

typedef string Name;

#endif
